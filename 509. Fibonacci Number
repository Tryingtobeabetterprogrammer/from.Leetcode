class Solution:
    def fib(self, n: int) -> int:
        if n==0:
            return 0
        if n==1:
            return 1
        
        fib=[0,1]
        i=2
        while i<=n:
            fib.append(fib[i - 1] + fib[i - 2])
            i+=1
        return fib[n]
